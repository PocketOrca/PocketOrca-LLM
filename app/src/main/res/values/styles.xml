<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">#0e1116</item>
        <item name="android:navigationBarColor">#0e1116</item>
        <!-- API 35+ honors this opt-out from forced edge-to-edge; older
             versions skip the unknown attr. Keeps content below the
             status bar on Android 15 (v0.5.1 fix). -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>
